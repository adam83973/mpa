<% unless @student || @lead || @opportunity %>
  <ul class="list-unstyled">
    <% @user.notes.includes(:user, :notable).order("created_at DESC").each do |note| %>
      <li>
        <%= render '/shared/note_template', note: note %>
      </li>
    <% end %>
  </ul>
<% end %>