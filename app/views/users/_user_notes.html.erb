<% unless @student || @opportunity || @lesson %>
  <ul class="list-unstyled">
    <% @note_user.notes.includes(:user, :notable).order("created_at DESC").each do |note| %>
      <li>
        <%= render '/shared/note_template', note: note %>
      </li>
    <% end %>
  </ul>
<% end %>
