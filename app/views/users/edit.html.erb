<h1>Edit User</h1>

<% if current_user.admin? %>
	<% unless @contacts.nil? || @contacts.empty? %>
		<%= render 'search' %>
	<% end %>
<% end %>

<%= render 'form' %>


<div class="container">
	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>
</div>
