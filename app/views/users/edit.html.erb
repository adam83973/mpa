<h1>Edit User</h1>

<div class="row">
	<div class="span4">
		<%= render 'form' %>
	</div>
	<div class="span8">
	<% if current_user.admin? %>
		<% unless @contacts.nil? %>
			<%= render 'search' %>
		<% end %>
	<% end %>
	</div>
</div>


<div class="container">
	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>
</div>