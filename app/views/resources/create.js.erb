<% if @resource.new_record? %>
  alert("Failed to upload resource: <%= j @resource.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#resources").append("<%= j render(@resource) %>");
<% end %>
