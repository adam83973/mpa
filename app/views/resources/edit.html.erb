<h1>Edit Resource</h1>

<%= link_to image_tag(@resource.file_url(:thumb)), @resource.file.url %><br /><br />

<%= render 'form' %>

<%= link_to 'Show', @resource, class: 'm-btn' %>
<%= link_to 'Back', resources_path, class: 'm-btn' %>
