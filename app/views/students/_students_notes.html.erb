<% unless @note_user || @opportunity || @user %>
  <ul class="list-unstyled">
    <% @student.notes.includes(:user, :notable).order("created_at DESC").each do |note| %>
      <li>
        <%= render '/shared/note_template', note: note %>
      </li>
    <% end %>
  </ul>
<% end %>