<% if params[:type] == 'problem' || params[:problem][:activity_type] == 'Problem' %>
	<h1>New Problem</h1>
	<%= render 'problem_form' %>
<% else %>
	<h1>New Activity / Game</h1>
	<%= render 'activity_form' %>
<% end %>

<%= link_to 'Back', problems_path %>
