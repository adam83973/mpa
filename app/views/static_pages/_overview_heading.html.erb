<div class='row'>
  <div class="col-sm-8 col-xs-7">
    <h3 class="card-title" style="display:inline;">Today's Overview</h3>
  </div>
  <div class="col-sm-4 col-xs-5">
    <%= simple_form_for :location do |f| %>
      <%= f.input :id, collection: Location.all, label_method: :name, selected: @user_location.id, label: false, input_html: { data: {url: root_url}}  %>
    <% end %>
  </div>
</div>
