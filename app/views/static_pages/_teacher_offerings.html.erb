<% if class_session.in_session? %>
<% else %>
<div class="classform">
  <h3>Start Class!</h3>
    <%= form_tag(controller: "class_sessions", action: "start_class", method: "post") do %>
      <%= label_tag "Class" %>
      <%= collection_select :offering, :id, @offerings, :id, :offering_name %>
      <%= label_tag "Week" %>
      <%= text_field_tag :week, params[:week] %>
      <p>
        <%= submit_tag "Start Class", id: :startclass, disabled: :disabled, class: "m-btn green" %>
      </p>
</div>
  <% end %>
<% end %>


<div id='class_in_session'>
  <div class="row">
    <div class="span9">
      <%= render 'class_in_session' %>
    </div>
  </div>
</div>

<%= render 'attendance_modal' %>

<%= render 'grades_modal' %>

<%= render 'class_summary_modal' %>

<%= render 'attendance_grades_buttons' %>

<div class="row-fluid">
  <div class="span8">
    <%= render 'teachers_offerings' %>
  <div>
<div>
