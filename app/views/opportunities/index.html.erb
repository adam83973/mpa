<h1>Listing opportunities</h1>

<table>
  <tr>
    <th>Registration</th>
    <th>Student</th>
    <th>Admin</th>
    <th>Offering</th>
    <th>Attended trial</th>
    <th>Trial date</th>
    <th>Status</th>
    <th>Possible restart date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @opportunities.each do |opportunity| %>
  <tr>
    <td><%= opportunity.registration_id %></td>
    <td><%= opportunity.student_id %></td>
    <td><%= opportunity.admin_id %></td>
    <td><%= opportunity.offering_id %></td>
    <td><%= opportunity.attended_trial %></td>
    <td><%= opportunity.trial_date %></td>
    <td><%= opportunity.status %></td>
    <td><%= opportunity.possible_restart_date %></td>
    <td><%= link_to 'Show', opportunity %></td>
    <td><%= link_to 'Edit', edit_opportunity_path(opportunity) %></td>
    <td><%= link_to 'Destroy', opportunity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Opportunity', new_opportunity_path %>
