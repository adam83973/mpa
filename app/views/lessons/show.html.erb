<p id="notice"><%= notice %></p>

<p>
  <h2><%= @lesson.name %></h2>
</p>

<p>
  <b>Week:</b>
  <%= @lesson.week %>
</p>

<p>
  <b>Standard:</b>
  <%= @lesson.standard.name if @lesson.standard %>
</p>

<p>
  <b>Course:</b>
  <%= @lesson.standard.course.course_name if @lesson.standard %>
</p>

<p>
  <b>Assignment:</b>
  <% if @lesson.assignment %>
  <%= link_to resource_lookup(@lesson.assignment).filename, resource_lookup(@lesson.assignment) unless @lesson.assignment == '#' %>
  <% end %>
</p>

<p>
  <b>Assignment key:</b>
  <%= link_to resource_lookup(@lesson.assignment_key).filename, resource_lookup(@lesson.assignment) if @lesson.assignment_key %>
</p>

<p>
  <b>Activities/Games/Problems:</b>
  <% if @lesson.problems %>
  <% @lesson.problems.each do |problem| %>
  <%= link_to "#{problem.title}", problem %>
  <% end %>
  <% else %>
    There are no activities associated with this lesson.
  <% end %>
</p>

<p>
  <b>Videos:</b>
</p>

  <% @lesson.videos.each do |video| %>
    <p>
      <%=raw YouTubeAddy.youtube_embed_url("#{video.url}",420,315) if video.url %>
    </p>
  <% end %>



<%= link_to 'Edit', edit_lesson_path(@lesson) %> |
<%= link_to 'Back', lessons_path %>
