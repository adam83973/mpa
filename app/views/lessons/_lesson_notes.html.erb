<% if @lesson %>
  <ul class="list-unstyled">
    <% @lesson.notes.order("created_at DESC").each do |note| %>
      <li>
        <%= render '/shared/note_template', note: note %>
      </li>
    <% end %>
  </ul>
<% end %>
