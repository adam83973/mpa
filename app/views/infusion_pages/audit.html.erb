<h3>Payment Audit Report for Active Students</h3>
<div class="row">
	<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
		<table class="table table-hover table-bordered table-condensed table-responsive audit">
			<%= link_to "Load Attendance", '#', class: "btn btn-default pull-right", id: 'load_attendance' %>
			<thead>
				<tr>
					<th></th>
					<th>Parent Name</th>
					<th>Active Students</th>
					<th>Active Subscriptions</th>
					<th>Balance Due</th>
				</tr>
			</thead>
			<tbody>
				<% @parents.each_with_index do |parent, i| %>
					<% if parent.active_students? %>
						<tr <%= "class=danger" if parent.balance_due != 0 %>>
							<td class="text-center"><%= i + 1 %></td>
							<td ><%= link_to parent.full_name, parent, :target => "_blank" %> <%= "- " + parent.billing_note if !parent.billing_note.empty? %></td>
							<td>
								<ol>
									<% parent.active_registrations.each do |reg| %>
										<li class='active-registration' data-student-id="<%= reg.student_id %>">
											<%= reg.student.full_name %> <%# last_attendance ? last_attendance.created_at.strftime("%D") : "No Attendance" %>
										</li>
									<% end %>
								</ol>
							</td>
							<td class="text-center"><%= parent.subscription_count %></td>
							<td class="text-center">$<%= parent.balance_due %></td>
						</tr>
					<% end %>
				<% end %>
			</tbody>
		</table>

		<!-- <h1>Parents With Registration and No Subscription</h1>
		<table class="table table-striped table-bordered">
			<thead>
				<tr>
					<th>
						Parent
					</th>
						<th>
							Registrations (status)
						</th>
				</tr>
			</thead>
			<tbody>
				<%# @parents.each do |parent| %>
					<%# if parent.active_students? == true && !parent.active_subscription? %>
						<tr>
							<td>
								<%# link_to parent.full_name, parent, target: '_blank' %> <%# "- " + parent.billing_note if !parent.billing_note.empty? %>
							</td>
							<td>
								<ol>
									<%# parent.registrations.each do |reg| %>
										<%# if (0..2) === reg.status %>
											<li><%# reg.student.full_name %> <%# render '/students/registration_dates', reg: reg %></li>
										<%# end %>
									<%# end %>
								</ol>
							</td>
						</tr>
					<%# end %>
				<%# end %>
			</tbody>
		</table> -->
		<%= link_to 'Back', infusion_pages_home_path, class: 'm-btn m-btn-small' %>
		<br />
	</div>
</div>
