<h2>Today's Assessments</h2>
<table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>Day</th>
        <th>Parent</th>
        <th>Time</th>
      </tr>
    </thead>
    <tbody>
        <% appointments.each do |appointment| %>
          <tr>
            <td>
              <%= appointment.time.strftime("%A") %>
            </td>
            <td>
              <%= link_to appointment.user.full_name, appointment.user %>
            </td>
            <td>
              <%= appointment.time.getlocal.strftime("%I:%M %P") %>
            </td>
          </tr>
        <% end %>
    </tbody>
</table>
